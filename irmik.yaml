app:
  name: go-irmik-docs
  env: development
  baseURL: http://localhost:8080

server:
  host: 0.0.0.0
  port: 8080

cache:
  driver: memory

islands:
  enabled: false

auth:
  jwtIssuer: go-irmik-docs
  # Outside development, set IRMIK_JWT_SECRET (see docker-compose.yml).
