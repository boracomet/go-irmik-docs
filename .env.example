# Required when IRMIK_ENV=production (Docker / Dokploy).
IRMIK_JWT_SECRET=change-me-to-a-long-random-string

# Optional: public site origin for absolute og:image / twitter:image URLs.
# Social crawlers need an absolute URL; without this, /assets/og.png stays relative.
SITE_URL=https://docs.example.com
